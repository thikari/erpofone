mixin avatar(agent, size='md')
  - const sizes = { sm: 'w-6 h-6 text-[10px] rounded-md', md: 'w-9 h-9 text-xs rounded-lg', lg: 'w-12 h-12 text-base rounded-xl' }
  div(class=`flex items-center justify-center font-bold flex-shrink-0 av-${agent.color} ${sizes[size]}`)= agent.initials
